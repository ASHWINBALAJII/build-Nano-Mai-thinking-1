"""Model + training configuration and a param-count check.

TODO(you): a dataclass with the nano config (~50-60M params) and a helper to
print the parameter count so we can confirm the budget. See configs/nano.yaml.
"""
